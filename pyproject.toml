[project]
name = "to-amenities-explorer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "flask-cors>=6.0.5",
    "gunicorn>=26.2.0",
    "jsonify>=0.5",
    "python-dotenv>=1.2.3",
]

[tool.uv.workspace]
members = [
    "myproject",
]
